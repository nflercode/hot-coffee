import React from 'react';

function App() {
  // const data = fetch('http://localhost:3000/arguments').then(data => data.json(), (err) => console.error(err)).then(a => console.log(JSON.stringify(a)));
  return (
    <div className="App">
      <header className="App-header">
        <h1>nfler - arguing is healthy</h1>
        <i>Fat redesign ( v.2 )</i>
        <b>\o/</b>
      </header>
    </div>
  );
}

export default App;
